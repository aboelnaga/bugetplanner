{
  "mcpServers": {
    "supabase": {
      "command": "npx",
      "args": ["supabase", "start"],
      "env": {
        "SUPABASE_ACCESS_TOKEN": "${SUPABASE_ACCESS_TOKEN}",
        "SUPABASE_PROJECT_ID": "${SUPABASE_PROJECT_ID}"
      }
    }
  }
} 